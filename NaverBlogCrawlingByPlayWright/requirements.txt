# 네이버 블로그 크롤링 도구 필수 패키지
# 설치: pip install -r requirements.txt

# HTTP 요청
requests>=2.28.0

# HTML 파싱
beautifulsoup4>=4.11.0

python-dotenv>=1.0.0

# AI 글작성 (OpenAI / Google Gemini)
openai>=1.40.0
google-generativeai>=0.8.0

# AI 완성본 마크다운 → HTML 변환
markdown>=3.5

# 브라우저 모드 (--mode browser): undetected-chromedriver + Selenium
selenium>=4.15.0
undetected-chromedriver>=3.5.0

# 단일 exe 빌드(선택): pip install pyinstaller && python build_exe.py
#   → release/<프로젝트폴더명>/<프로젝트폴더명>.exe 및 .env·지침·설정 파일 동봉
